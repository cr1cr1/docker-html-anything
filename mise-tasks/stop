#!/usr/bin/env bash
#MISE description="Stop and remove the local container"
set -euo pipefail

NAME="${NAME:-ha}"
docker stop "${NAME}" >/dev/null 2>&1 || true
docker rm "${NAME}" >/dev/null 2>&1 || true
echo "Container '${NAME}' stopped and removed."

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: &name html-anything
  # annotations:
  #   haproxy-ingress.github.io/backend-protocol: h2
  #   cert-manager.io/cluster-issuer: issuer-name
  #   external-dns.alpha.kubernetes.io/target: example.com
spec:
  ingressClassName: nginx
  rules:
    - host: &host ha.example.com
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: *name
                port:
                  name: http
  tls:
    - hosts:
        - *host
      secretName: html-anything-tls

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: html-anything
spec:
  rules:
    - host: ha.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: html-anything
                port:
                  number: 80

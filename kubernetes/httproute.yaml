apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: html-anything
spec:
  hostnames:
    - ha.example.com
  parentRefs:
    - name: html-anything
  rules:
    - backendRefs:
        - name: html-anything
          port: 80
